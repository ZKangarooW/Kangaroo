#ifndef __SHOW_H__
#define __SHOW_H__

#include "include.h"

/**********宏定义************/
/**********结构体************/
/**********全局变量声明*******/
#define ABS(x) ((x) > 0 ? (x) : -(x)) // 取绝对值

/**********全局函数**********/

void Show_Display(void);

#endif
